"""CodeToWiki: turn code into structured, indexed Wiki documentation.

This package contains the reference-index tooling only (no external knowledge
base / semantic index). It builds and incrementally updates a ``metadata.json``
that maps source files <-> wiki pages via ``<cite>`` / 章节来源 citations.
"""

__version__ = "0.1.0-beta"
